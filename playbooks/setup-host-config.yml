- name: Safely append host mappings to /etc/hosts
  hosts: 
  - all
  become: yes
  gather_facts: no

  vars:
    hosts_mapping:
      master01: 
      


  tasks:
    - name: Ensure each mapping exists in /etc/hosts (append if missing)
      lineinfile:
        path: /etc/hosts
        line: "{{ item.value }} {{ item.key }}"
        state: present
        insertafter: EOF
      loop: "{{ hosts_mapping | dict2items }}"
