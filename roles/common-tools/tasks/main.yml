- name: Install common network tools (ping, telnet, curl, etc.)
  package:
    name: "{{ common_nettools_packages }}"
    state: present
  vars:
    common_nettools_packages: >-
      {{ 
        ['iproute', 'net-tools', 'telnet', 'curl', 'wget', 'bind-utils'] 
        if ansible_os_family == 'RedHat' 
        else ['iproute2', 'net-tools', 'telnet', 'curl', 'wget', 'dnsutils'] 
      }}
  tags: nettools

- name: Ensure common text processing tools (vim, nano, etc.) are installed
  package:
    name: "{{ common_texttools_packages }}"
    state: present
  vars:
    common_texttools_packages: >-
      {{ 
        ['vim', 'nano', 'jq'] 
        if ansible_os_family == 'RedHat' 
        else ['vim', 'nano', 'jq'] 
      }}
  tags: texttools

- name: Ensure git is installed
  package:
    name: git
    state: present
  tags: git   

- name: Ensure common utility tools (unzip, tar, etc.) are installed
  package:
    name: "{{ common_utility_packages }}"
    state: present
  vars:
    common_utility_packages: >-
      {{ 
        ['unzip', 'tar', 'rsync'] 
        if ansible_os_family == 'RedHat' 
        else ['unzip', 'tar', 'rsync'] 
      }}
  tags: utilitytools

